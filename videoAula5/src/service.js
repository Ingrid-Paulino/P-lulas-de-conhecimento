// exemplo na camada de service

const service = {
  /** Tipo que meu getAll retorna isso, e eu vou conseguir pegar facil essas chaves em outras camadas como a que eu fiz la na controller
   * @return { Array<{
   *   id: number
   *   neme: string
   * }>}
   */
  getAll() {

  },

  /**
   * @return { Array<{
   *   id: number
   *   neme: string
   *   age: number
   *   address: string
   * }>}
   */

  create() {

  },
}

module.exports = service;